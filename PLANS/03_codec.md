# 计划 03 · CODEC 提取

状态：**容器格式已打通，台词已全量提取且 100% 可解码**
（2,049 记录 / 24,438 行 / 0 个替换字符）。
**语言归属未解决**——这是当前唯一的阻塞项。

完整取证见 [../ANALYSIS/03_codec.md](../ANALYSIS/03_codec.md)。

## 已完成

- [x] 确认 CODEC = BRIEFING 子系统（`sub_1408C57D0` 建立 `"codec thread"`）
- [x] 拿到解密密钥：汇编核对 `0x1400A5656-0x1400A567C`
      `key = name_hash("0076531d.DAT") = 0xC4CECBE0`
- [x] **实证磁盘上是逐扇区（4096 B）分别加密**（`_probe_bri8.py` A/B 判决：
      1519 vs 579 个可解码窗口；`file+0x1700` 处解出记录头魔数）
- [x] 证否"块内 keystream 偏移"模型（`_probe_bri17.py`：k>0 全文件 0 魔数）
- [x] 从 `sub_1400A3230` 推出记录头（28 字节）并全文件扫描出记录
- [x] 用"脚本区入口 opcode 必须 0x8d/0x8e"剔除 601 条魔数碰撞假阳性
      （`_probe_bri18.py`：入口合法 2049 条 ↔ 台词全可解码，完全一致）
- [x] 台词区（u32 偏移表 + NUL 结尾 UTF-8 文本池）全部解出
      → `ANALYSIS/_briefing_lines.tsv`（2,049 记录 / 24,438 行 / **0 乱码**）
- [x] 定位字节码操作数解码器 `sub_1400A4770` 与解释器 `sub_1400A35C0`
      （`0x30` 文本 / `0x60` 回调 / `0x70` 子程序）
- [x] 枚举语音资源 ID（414 种：`v_bri` / `v_fop` / `v_myo` / `v_paz` …）
- [x] 枚举富文本标记 `<R=表示,よみ>`（270 种 / 473 次）
- [x] 取证 `lang_get_language_id()`：0=en 1=fr 2=de(gr) 3=it 4=es/pt 6=ja

## 未完成（按优先级）

### 1. 语言归属（阻塞"可用化"）

已排除：记录头内没有语言字段（各扇区段记录头逐字节同构）。

已定位的机制：语言只体现在寻址参数上——请求字
`idx(bit0-7) | 扇区(bit8-23) | (页数-1)(bit24-31)`，来自
`sub_1408045E0(ctx, a2)` 的 `a2`。

- [ ] 定位 `dword_141495DE0` 的填充者（`sub_1405E06E0` @ `0x1405E07CB`
      读取；静态全 `0xFFFFFFFF`，运行时填充）
- [ ] 追 `sub_1408045E0` 的 `a2` 在 `sub_14062C8D0`（`a1[8].m128i_u32[0]`，
      即 `a1+128`）与 `sub_1405E06E0` 中的来源
- [ ] 找到语言 ID → 扇区基址的映射后回填 `Record.lang`

> 已证伪、勿再尝试：按语音 ID 做跨段对齐（段间 Jaccard 0.55–0.67，
> 跨段共享 ID 的记录台词条数 0/200 一致）。

### 2. 逐扇区解密与 `buffer_xor_decrypt` 的矛盾

`briefing_dat_load` 解密长度是 `dword_14117C094 << 12`，而该值
`(HIBYTE(req)+1) << 2 >= 4`（`0x14080426D` 起汇编），与逐扇区事实冲突。

- [ ] 定位 op=7 的 I/O 工作线程（`sub_14045DF30(req, 7, ...)`
      → `sub_14045D120` 提交），确认逐扇区解密的实现位置

### 3. 字节码语义

- [ ] 反 `sub_14013CD90`（op `0x30` 的处理）确认文本取用方式
- [ ] 确认 `0x6d` 指令（u24 id `0x3B91EB`，长度 25）各参数语义
      —— 已发现参数第 7 字节是 0 起的行号
- [ ] 建立「语音 ID ↔ 台词」的**逐行**对应（当前一条记录通常只显式
      出现 1 个语音 ID，其余行号在参数里）

### 4. 富文本标记

- [ ] 枚举 `<R=...>` 全部 270 种取值并与 olang 的 `<I=...>` 建对照表
- [ ] 定位消费 `<R=>` 的渲染函数，确认「表示 / よみ」两侧的用法

### 5. 扇区块语义

观察到 `x2` 段（扇区 ~895–1011，`v_fop` / `v_myo` 集）与其它段的语音 ID
交集几乎为 0（仅 es 有 19 个）。
- [ ] 确认该段是另一套内容还是同一套的另一形态

## 复现

```powershell
cd d:\PWSF\TOOLS
python pwsf_briefing.py -o ..\ANALYSIS\_briefing_lines.tsv
```
